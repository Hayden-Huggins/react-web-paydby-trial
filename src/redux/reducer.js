import user from '../data/user'
// reducer for user, currently working on state to store integration
const userReducer = function User(state = user, action)
{
    return state
}

export default userReducer